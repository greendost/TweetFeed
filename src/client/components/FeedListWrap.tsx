import React, { Component } from 'react';
import FeedListHeader from './FeedListHeader';
import FeedList from './FeedList';

const FeedListWrap = () => (
  <div>
    <FeedListHeader />
    <FeedList />
  </div>
);

export default FeedListWrap;
